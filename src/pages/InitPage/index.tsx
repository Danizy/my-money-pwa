import React from 'react';
import Loader from '../../components/Loader';

const InitPage = () => {
  return (
    <div>
      <Loader />
      <Loader />
      <Loader />
    </div>
  );
};

export default InitPage;
